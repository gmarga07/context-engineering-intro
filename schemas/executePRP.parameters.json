{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "executePRP parameters",
  "type": "object",
  "properties": {
    "prpPath": {
      "type": "string",
      "description": "Path to the PRP markdown file to execute validation for"
    },
    "stopOnFailure": {
      "type": "boolean",
      "description": "Stop executing further commands on first failure"
    }
  },
  "required": ["prpPath"],
  "additionalProperties": false
}